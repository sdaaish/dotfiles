#!/usr/bin/env bash

# Workaround for browser support in WSL
FIREFOX="/c/Program Files/Mozilla Firefox/firefox.exe"

if [ -f "${FIREFOX}" ]
then
    "${FIREFOX}" --new-tab "${@}"
elif [ -f "/mnt${FIREFOX}" ]
then
    "/mnt${FIREFOX}" --new-tab "${@}"
else
    exit 1
fi
exit 0
