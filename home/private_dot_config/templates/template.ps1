<#
.SYNOPSIS
.DESCRIPTION
#>
function `(capitalize (file-name-nondirectory (file-name-sans-extension (buffer-file-name))))` {
    [CmdletBinding()]
    param(
        $1
    )

    process {
        $0
    }
}
